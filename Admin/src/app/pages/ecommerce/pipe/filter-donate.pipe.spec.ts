import { FilterDonatePipe } from './filter-donate.pipe';

describe('FilterDonatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDonatePipe();
    expect(pipe).toBeTruthy();
  });
});
